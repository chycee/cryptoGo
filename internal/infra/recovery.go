package infra

import (
	"log/slog"
	"os"
	"runtime/debug"
)

// Recover는 패닉으로부터 복구하고, 스택 트레이스를 로깅한 뒤 종료합니다.
// 이 함수는 메인 엔트리 포인트나 중요한 고루틴의 최상단에서 defer로 호출되어야 합니다.
func Recover() {
	if r := recover(); r != nil {
		// 1. 스택 트레이스 캡처
		stack := string(debug.Stack())

		// 2. 스택과 함께 심각한 에러 로깅
		// 구조화된 로깅을 사용하여 파싱 가능하게 함.
		slog.Error("🚨 CRITICAL PANIC RECOVERED",
			"error", r,
			"stack", stack,
		)

		// 3. 종료
		// Sequencer 아키텍처에서 핫패스(hotpath) 내의 패닉은 미정의 상태를 의미함.
		// 데이터 오염을 방지하기 위해 반드시 크래시(종료)해야 함.
		os.Exit(1)
	}
}
